# BOM/encoding — seed files must be LF, no BOM
*.seed text eol=lf working-tree-encoding=UTF-8

# Build artifacts
*.log
*.tar.gz
build-info.json
.last_sha_*

# OS
.DS_Store
Thumbs.db
.codegraph/
.reasonix/

# 临时对比/工具脚本（不入库）
scripts/
