# BOM/encoding — seed files must be LF, no BOM
*.seed text eol=lf working-tree-encoding=UTF-8

# Build artifacts
*.log
*.tar.gz
build-info.json
.last_sha_*

# OS
.DS_Store
Thumbs.db
.codegraph/
.reasonix/
.diagnostics/

# 本地工具状态（GitHub CLI 等，不入库）
.local/

# 临时对比/工具脚本（不入库）
/scripts/
